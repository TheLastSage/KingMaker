#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../meteor-npm/bin/meteor-npm" "$@"
  ret=$?
else 
  node  "$basedir/../meteor-npm/bin/meteor-npm" "$@"
  ret=$?
fi
exit $ret
